# ========================================
# PRISM Phase 2 - 의존성 패키지
# ========================================
# 기존 POC 패키지 + Layout Detection 추가

# ========== Web Framework ==========
streamlit==1.31.0
fastapi==0.109.0
uvicorn==0.27.0

# ========== PDF Processing ==========
PyMuPDF==1.23.8  # fitz
pdf2image==1.16.3
pdfplumber==0.10.3

# ========== OCR ==========
paddlepaddle==3.0.0
paddleocr==2.8.1

# ========== Image Processing ==========
opencv-python-headless==4.9.0.80
Pillow==10.2.0

# ========== Layout Detection (NEW!) ==========
# Option 1: LayoutParser (권장)
layoutparser==0.3.4
torchvision==0.17.0
torch==2.2.0

# Option 2: Detectron2 (고급)
# detectron2 @ git+https://github.com/facebookresearch/detectron2.git

# ========== Table Detection (NEW!) ==========
# Table Transformer
transformers==4.37.2
timm==0.9.12

# ========== Text Chunking (NEW!) ==========
langchain==0.1.7
langchain-community==0.0.20
# sentence-transformers==2.5.1  # 임베딩 제외 (Phase 3에서 필요시 활성화)
tiktoken==0.6.0

# ========== VLM Providers ==========
# Claude
anthropic==0.18.1

# Azure OpenAI
requests==2.31.0

# Ollama (local)

# ========== Vector Store (NEW!) ==========
# chromadb==0.4.22  # Phase 3에서 활성화
# qdrant-client==1.7.3  # 프로덕션용

# ========== Database ==========
# SQLite는 Python 내장

# ========== Utilities ==========
python-dotenv==1.0.0
tqdm==4.66.2
pydantic==2.6.1

# ========== Testing ==========
pytest==8.0.2
pytest-cov==4.1.0

# ========================================
# 설치 가이드 (Phase 2)
# ========================================
#
# 1. 기존 환경 백업 (옵션)
#    cp requirements.txt requirements_poc_backup.txt
#
# 2. Phase 2 패키지 설치
#    pip install -r requirements_phase2.txt
#
# 3. Layout Detection 모델 다운로드
#    python -c "import layoutparser as lp; lp.Detectron2LayoutModel('lp://PubLayNet/faster_rcnn_R_50_FPN_3x/config')"
#
# 4. (임베딩 제외 - Phase 3에서 필요시 추가)
#
# ========================================
# 주의사항
# ========================================
#
# - Torch 패키지는 큰 용량 (>1GB)
# - GPU 사용 시 CUDA 버전 확인 필요
# - Detectron2는 빌드 시간 오래 걸림 (선택적)
#